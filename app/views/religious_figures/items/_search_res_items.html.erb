<% if @res.present? && @res.count > 0 %>
  <% @res.items.each do |item| %>
    <div class="searchResults buffer-bottom-sm">
      <h3><%= search_item_link(item) %></h3>
      <div class="row">
        <div class="<%= item["highlight"].present? ? "col-md-4" : "col-md-12" %> search-results-info">
          <ul>
            <% if item["date_display"].present? %>
              <li>
                <%= metadata(item, t("search.results.item.date",
                  default: "Lifespan"), "date_display", link: false) %>
              </li>
            <% end %>
            <%# TODO how are we going to structure this metadata? Person within person is weird %>
            <% if item["type"].present? %>
              <li>
                <%= metadata(item, t("search.results.item.type",
                  default: "Religious Tradition"), "type", link: false) %>
              </li>
            <% end %>
            <% if item["rdf"].present? %>
              <li class="associated-monasteries">
                <span class="array-label"><strong>Associated Monasteries<% if item["monastery_count_k"].present? %> (<%= item["monastery_count_k"] %>)<% end %>:</strong></span>
                <ul>
                <% monasteries = item["rdf"].select { |i| i["predicate"] != "sameAs" } %>
                <% monasteries.each do |monastery| %>
                  <% id = parse_md_parentheses(monastery["object"]) %>
                  <% name = parse_md_brackets(monastery["object"]) %>
                    <li>
                      <% if name %>
                        <%= link_to name, monasteries_item_path(id) %>
                      <% else %>
                        <%= name %>
                      <% end%>
                    </li>
                  <% end %>
                </ul>
              </li>
            <% end %>
          </ul>
        </div>
        <% if item["highlight"].present? %>
          <div class="col-md-8 search-results-text">
            <% item["highlight"].each do |key, hls| %>
              <div class="search-results-highlights well">
                <strong><%= key.titleize %>:</strong>
                <% hls.each do |hl| %>
                  <p><%= hl.html_safe %></p>
                <% end %>
              </div>
            <% end %>
          </div>
        <% end %>
      </div>
    </div>
  <% end %>
<% end %>
